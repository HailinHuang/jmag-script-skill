# Promotion lifecycle

`observed -> candidate -> verified -> approved -> stable`, with rejection allowed before stable. Static inspection creates only `observed`. Verification evidence must identify tests, JMAG version, Help sources, and runtime result. Approval is an explicit user decision. Fresh tests are required after approval before the stable catalog changes.
