# Implementation Plan After Milestone 0

1. Repository alignment: keep documented contracts, package metadata, catalog,
   CLI, and tests aligned with the committed tree.
2. Existing-project opening and protected copies: build on the current project
   lifecycle APIs with isolated evidence.
3. Geometry vertical slice: verify a small Geometry workflow before broader
   machine construction.
4. Parameter updates and explicit case execution: extend evidence from the
   existing library without implicit result or Study behaviour.
5. V-IPM reference workflow: define it as a future, evidence-backed reference.
6. Only after those steps, evaluate DesignSpec executor, offline workflow, and
   PySide6 frontend work.

Executor, offline, frontend, and `inspect-jmdl` paths are planned—not present
in this commit. No command example here implies that they can run.
